Contains simulation source code for both CIM drivers and although wraps memory modules into `CIMMemoryDevice` treated by those drivers.

TODO: make this compile & use it in the compiler - make driver perform CIM-Ops whenever the address is within the allocated range
